<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#part.partKey}}Edit Part{{/part.partKey}}{{^part.partKey}}Add New Part{{/part.partKey}}</title>
    <link rel="stylesheet" href="/css/main.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/">Home</a></li>
                <li class="breadcrumb-item"><a href="/parts">Parts</a></li>
                <li class="breadcrumb-item active">
                    {{#part.partKey}}Edit Part{{/part.partKey}}
                    {{^part.partKey}}Add New Part{{/part.partKey}}
                </li>
            </ol>
        </nav>

        <div class="card">
            <div class="card-header">
                <h2>
                    {{#part.partKey}}Edit Part{{/part.partKey}}
                    {{^part.partKey}}Add New Part{{/part.partKey}}
                </h2>
            </div>
            <div class="card-body">
                <form action="/parts/save" method="post">
                    <!-- Hidden field for part key (for edit mode) -->
                    {{#part.partKey}}
                    <input type="hidden" name="partKey" value="{{part.partKey}}">
                    {{/part.partKey}}

                    <div class="mb-3 row">
                        <label for="partCost" class="col-sm-2 col-form-label">Part Cost ($)</label>
                        <div class="col-sm-10">
                            <input type="number" step="0.01" class="form-control" id="partCost" name="partCost" 
                                value="{{part.partCost}}" required>
                        </div>
                    </div>

                    <div class="mb-3 row">
                        <label for="partSupplier" class="col-sm-2 col-form-label">Supplier</label>
                        <div class="col-sm-10">
                            <input type="text" class="form-control" id="partSupplier" name="partSupplier" 
                                value="{{part.partSupplier}}" required>
                        </div>
                    </div>

                    <div class="mb-3 row">
                        <div class="col-sm-10 offset-sm-2">
                            <button type="submit" class="btn btn-primary">Save Part</button>
                            <a href="/parts" class="btn btn-secondary">Cancel</a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>